// ===================== STUDENT CONFIG =====================
// Paste your custom code only inside the marked zones below.

window.STUDENT_SEGMENTS = function (t) {
    // ===================== SEGMENTS PASTE ZONE =====================

    // ===============================================================
    return '';
};

window.STUDENT_SIGNALS = function (api) {
    // ===================== SIGNALS PASTE ZONE =====================

    // =============================================================
}; 